{
  "type": "module",
  "name": "tank",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "build:operator": "spago bundle-app --watch --main Operator --to operator-bundle.js",
    "build:tank": "spago bundle-app --watch --main Tank --to tank-bundle.js",
    "build:standalone": "spago bundle-app --watch --main Standalone --to standalone-bundle.js",
    "start": "node index.js --port 3000",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^7.15.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-plugin-import": "^2.22.1",
    "purescript": "^0.13.8",
    "spago": "^0.17.0"
  },
  "dependencies": {
    "express": "^4.17.1",
    "ws": "^7.4.1"
  }
}
